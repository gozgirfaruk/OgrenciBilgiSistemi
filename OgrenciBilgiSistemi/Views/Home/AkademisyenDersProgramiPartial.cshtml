@{
    ViewBag.Title = "Ders Programı";
    ViewBag.Sayfa = "AkademisyenDersProgramiPartial";
}

@using OgrenciBilgiSistemi.Db

@model List<Ders>

<div class="col-12">
    <div class="row">
        <div class="col-6">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Pazartesi</h5>

                    <!-- Table with stripped rows -->
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">Ders Adı</th>
                                <th scope="col">Ders Saati</th>
                                <th scope="col">Dersi Veren Akademisyen</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (Ders item in Model)
                            {
                                if (item.Gun == 1)
                                {
                            <tr>
                                <th scope="row">@item.Id</th>
                                <td>@item.Adi</td>
                                <td>@item.Saat</td>
                                <td>@item.Akademisyen.Unvan @item.Akademisyen.AdiSoyadi</td>
                            </tr>
                                }
                            }

                        </tbody>
                    </table>
                    <!-- End Table with stripped rows -->

                </div>
            </div>
        </div>
        <div class="col-6">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Salı</h5>

                    <!-- Table with stripped rows -->
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">Ders Adı</th>
                                <th scope="col">Ders Saati</th>
                                <th scope="col">Dersi Veren Akademisyen</th>

                            </tr>
                        </thead>
                        <tbody>
                            @foreach (Ders item in Model)
                            {
                                if (item.Gun == 2)
                                {
                                    <tr>
                                        <th scope="row">@item.Id</th>
                                        <td>@item.Adi</td>
                                        <td>@item.Saat</td>
                                        <td>@item.Akademisyen.Unvan @item.Akademisyen.AdiSoyadi</td>
                                    </tr>
                                }
                            }
                        </tbody>
                    </table>
                    <!-- End Table with stripped rows -->

                </div>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-6">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Çarşamba</h5>

                    <!-- Table with stripped rows -->
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">Ders Adı</th>
                                <th scope="col">Ders Saati</th>
                                <th scope="col">Dersi Veren Akademisyen</th>

                            </tr>
                        </thead>
                        <tbody>
                            @foreach (Ders item in Model)
                            {
                                if (item.Gun == 3)
                                {
                                    <tr>
                                        <th scope="row">@item.Id</th>
                                        <td>@item.Adi</td>
                                        <td>@item.Saat</td>
                                        <td>@item.Akademisyen.Unvan @item.Akademisyen.AdiSoyadi</td>
                                    </tr>
                                }
                            }
                        </tbody>
                    </table>
                    <!-- End Table with stripped rows -->

                </div>
            </div>
        </div>
        <div class="col-6">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Perşembe</h5>

                    <!-- Table with stripped rows -->
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">Ders Adı</th>
                                <th scope="col">Ders Saati</th>
                                <th scope="col">Dersi Veren Akademisyen</th>

                            </tr>
                        </thead>
                        <tbody>
                            @foreach (Ders item in Model)
                            {
                                if (item.Gun == 4)
                                {
                                    <tr>
                                        <th scope="row">@item.Id</th>
                                        <td>@item.Adi</td>
                                        <td>@item.Saat</td>
                                        <td>@item.Akademisyen.Unvan @item.Akademisyen.AdiSoyadi</td>
                                    </tr>
                                }
                            }
                        </tbody>
                    </table>
                    <!-- End Table with stripped rows -->

                </div>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-6">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Cuma</h5>

                    <!-- Table with stripped rows -->
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">Ders Adı</th>
                                <th scope="col">Ders Saati</th>
                                <th scope="col">Dersi Veren Akademisyen</th>

                            </tr>
                        </thead>
                        <tbody>
                            @foreach (Ders item in Model)
                            {
                                if (item.Gun == 5)
                                {
                                    <tr>
                                        <th scope="row">@item.Id</th>
                                        <td>@item.Adi</td>
                                        <td>@item.Saat</td>
                                        <td>@item.Akademisyen.Unvan @item.Akademisyen.AdiSoyadi</td>
                                    </tr>
                                }
                            }
                        </tbody>
                    </table>
                    <!-- End Table with stripped rows -->

                </div>
            </div>
        </div>
    </div>

</div>